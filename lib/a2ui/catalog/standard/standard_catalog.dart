// Standard Catalog - uses GenUI's built-in CoreCatalogItems.
//
// GenUI 0.7.0 provides all 18 A2UI standard components:
// AudioPlayer, Button, Card, CheckBox, Column, DateTimeInput,
// Divider, Icon, Image, List, Modal, MultipleChoice, Row,
// Slider, Tabs, Text, TextField, Video.
//
// No need to reimplement these — they are already provided
// with full dataSchema + widgetBuilder by the genui package.

export 'package:genui/genui.dart' show CoreCatalogItems;
